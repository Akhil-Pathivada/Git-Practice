1. git commit --amend:
	The git commit --amend command is a convenient way to modify the most recent commit.

2. git reset -- filename:
	Rollbacks the git-add of the filename
something-modified by f1

something-f1
